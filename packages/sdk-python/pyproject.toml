[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atlas-synapse"
version = "0.2.0"
description = "Monitor AI agents with Atlas Synapse — trace, evaluate, alert"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
anthropic = [
    "anthropic>=0.49",
]
langchain = [
    "langchain-core>=0.2",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "anthropic>=0.49",
]

[project.urls]
Homepage = "https://atlassynapse.ai"
Repository = "https://github.com/atlassynapseai/MVP"

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_synapse"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Run as: PYTHONPATH=src python3 -m pytest tests/
